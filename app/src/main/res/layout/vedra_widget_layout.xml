<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:padding="12dp">

    <!-- Top Bar: Title & Online Status -->
    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="VEDRA"
            android:textColor="#C4B5FD"
            android:textSize="16sp"
            android:textStyle="bold" />

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:layout_width="8dp"
                android:layout_height="8dp"
                android:src="@android:drawable/presence_online" />

            <TextView
                android:id="@+id/widget_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:text="AI Ready • Tap mic to talk"
                android:textColor="#10B981"
                android:textSize="11sp" />
        </LinearLayout>
    </LinearLayout>

    <!-- Center Prompt -->
    <TextView
        android:id="@+id/widget_prompt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_header"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="60dp"
        android:text="How can Vedra help you today?"
        android:textColor="#9CA3AF"
        android:textSize="12sp"
        android:ellipsize="end"
        android:maxLines="1" />

    <!-- Mic Button on Right -->
    <ImageButton
        android:id="@+id/widget_mic_button"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="@drawable/widget_mic_bg"
        android:contentDescription="Activate Voice"
        android:src="@android:drawable/ic_btn_speak_now" />

</RelativeLayout>
